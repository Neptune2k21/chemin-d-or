services:
  api:
    build:
      context: .
      dockerfile: Dockerfile.dev
    entrypoint: []
    command: ["air", "-c", ".air.toml"]
    volumes:
      - .:/app
      - go_modules:/go/pkg/mod

volumes:
  go_modules:
